const Project = (name, description) => {
    const name = name;
    const todoLists = [];
    const description = description

    const addTodo = (todo) => {
        todoLists.push(todo);
    }
}

export default Project;